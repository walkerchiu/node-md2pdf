/**
 * Configuration management module exports
 */

export { ConfigManager } from './manager';

export type { IConfigManager, ConfigOptions, ConfigSchema } from './types';

export { defaultConfig, environmentMappings } from './defaults';
