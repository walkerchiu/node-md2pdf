/**
 * event-validation.base tests
 */

import * as eventValidationBase from '../../../../../src/core/domain/events/event-validation.base';

describe('event-validation.base', () => {
  it('should be defined', () => {
    expect(eventValidationBase).toBeDefined();
  });
});
