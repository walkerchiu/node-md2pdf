/**
 * levels tests
 */

import * as levels from '../../../../src/infrastructure/logging/levels';

describe('levels', () => {
  it('should be defined', () => {
    expect(levels).toBeDefined();
  });
});
